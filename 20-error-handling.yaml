- name: demo on error handling
  hosts: local
  connection: local 
  tasks:
  - name: to check user exist or not 
    ansible.builtin.command: id roboshop
    register: user_output
    ignore_errors: true 

  - name: to print output 
    ansible.builtin.debug:
     msg: "{{ user_output }}"
     
